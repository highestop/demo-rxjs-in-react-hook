import React from "react";
import * as ReactDOM from "react-dom";
import { App } from "./rxjs";

ReactDOM.render(
  <React.StrictMode>
    <App></App>
  </React.StrictMode>,
  document.getElementById("root")
);
